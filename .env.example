# Copy to .env and fill in real values.
# MODE controls inference: "openai" (cloud) or "local" (Ollama + faster-whisper).
MODE=openai
OPENAI_API_KEY=sk-your-key-here

# Optional: where temp audio files go (defaults to system temp dir).
# TEMP_DIR=/tmp